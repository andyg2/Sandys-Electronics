# Copy to .env to override docker-compose defaults.
# .env is gitignored.

# Port the app is exposed on (browser opens http://localhost:$APP_PORT)
APP_PORT=8080

# MariaDB
DB_DATABASE=edgedevices
DB_USER=root
DB_PASSWORD=rootpw
