## What this PR changes


## Why


## Testing

- [ ] `bash tests/smoke.sh https://inventory.local` (or your dev URL) passes
- [ ] If you touched the schema: `php inventory/seed.php` on a clean DB still works
- [ ] If you added a new project record: it renders cleanly at `/project.php?id=N` (Mermaid + code highlight + power supply + allocations)
- [ ] No em dashes or en dashes in changed files

## Anything reviewers should look at first?

